// lib/doctor_screen/widgets/patient_history.dart

import 'package:flutter/material.dart';

class PatientHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      // Placeholder content for patient history
      child: Text('Patient History Widget'),
    );
  }
}
